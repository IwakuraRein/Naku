#version 450

// layout(location = 0) out vec4 color;

// layout(location = 0) in vec3 fragPos;
// layout(location = 1) in vec3 lightPos;

void main() 
{	
	//gl_FragDepth = length(fragPos - lightPos) / 20.0;
}